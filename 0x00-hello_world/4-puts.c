#include <stdio.h>

/**
 * main - program prints a string
 *
 * Return:  0 afterwards
 */

int main(void)
{
	char str[] = "\"Programming is like building a multilingual puzzle";

	puts(str);
	return (0);
}
